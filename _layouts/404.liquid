---
layout: default
---
<section class="section content-col" style="padding-top: 80px; padding-bottom: 120px;">
  <h2>404 · not found</h2>
  <p style="font-size: var(--fs-body); color: var(--color-muted); max-width: 520px;">
    There's nothing at this URL. Head back <a href="{{ '/' | relative_url }}">home</a>, or check
    <a href="{{ '/publications/' | relative_url }}">publications</a>,
    <a href="{{ '/projects/' | relative_url }}">projects</a>,
    <a href="{{ '/notes/' | relative_url }}">notes</a>.
  </p>
</section>
